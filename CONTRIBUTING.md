# Руководство по внесению вклада

Спасибо за ваш интерес к проекту! Мы рады любому вкладу в развитие скрипта.

## Как внести вклад

### Сообщить об ошибке

Если вы нашли ошибку:

1. Убедитесь, что проблема еще не была сообщена, проверив [Issues](https://github.com/YOUR_USERNAME/3x-ui-auto-installer/issues)
2. Откройте новый issue, используя шаблон для багов
3. Предоставьте максимально подробную информацию:
   - Версия операционной системы
   - Версия ядра Linux
   - Полный вывод ошибки
   - Шаги для воспроизведения
   - Ожидаемое поведение

### Предложить улучшение

Если у вас есть идея для улучшения:

1. Проверьте, не было ли уже подобного предложения
2. Откройте issue с меткой "enhancement"
3. Опишите:
   - Что вы хотите добавить/изменить
   - Почему это будет полезно
   - Как это должно работать

### Внести код

#### Перед началом работы

1. Fork репозитория
2. Создайте новую ветку от `main`:
   ```bash
   git checkout -b feature/your-feature-name
   ```
   или
   ```bash
   git checkout -b fix/bug-description
   ```

#### Стандарты кода

**Bash скрипты:**
- Используйте 4 пробела для отступов
- Добавляйте комментарии к сложным участкам кода
- Используйте `set -e` для остановки при ошибках
- Проверяйте код с помощью [shellcheck](https://www.shellcheck.net/)
- Используйте понятные имена переменных (UPPER_CASE для глобальных)

**Пример:**

```bash
# Хорошо
print_msg() {
    echo -e "${GREEN}[INFO]${NC} $1"
}

# Плохо
p() {
    echo $1
}
```

#### Тестирование

Перед отправкой PR протестируйте скрипт:

1. На чистой установке Ubuntu 22.04
2. На чистой установке Debian 11
3. Убедитесь, что все оптимизации применяются корректно
4. Проверьте, что firewall настраивается правильно

#### Commit сообщения

Используйте понятные commit сообщения:

- `feat: добавлена поддержка CentOS`
- `fix: исправлена ошибка генерации ключей`
- `docs: обновлен README с новыми примерами`
- `refactor: улучшена функция оптимизации`

#### Pull Request

1. Убедитесь, что ваш код прошел все тесты
2. Обновите CHANGELOG.md, если необходимо
3. Обновите README.md, если добавили новые функции
4. Создайте Pull Request с подробным описанием:
   - Что изменено
   - Зачем это нужно
   - Как протестировано

## Стиль кодирования

### Bash

```bash
#!/bin/bash

# Используйте set -e для строгого режима
set -e

# Константы в UPPER_CASE
CONST_VALUE="value"

# Функции в snake_case
function_name() {
    local local_var="value"
    echo "$local_var"
}

# Всегда цитируйте переменные
echo "$variable"

# Используйте [[ ]] вместо [ ]
if [[ -f "$file" ]]; then
    echo "File exists"
fi

# Проверка статуса команды
if command -v tool &> /dev/null; then
    echo "Tool is installed"
fi
```

## Приоритеты разработки

### Высокий приоритет
- Исправление критических багов
- Проблемы безопасности
- Совместимость с новыми версиями 3X-UI

### Средний приоритет
- Новые оптимизации производительности
- Поддержка дополнительных дистрибутивов
- Улучшение документации

### Низкий приоритет
- Косметические изменения
- Рефакторинг (без улучшения функциональности)

## Вопросы?

Если у вас есть вопросы:

1. Проверьте [FAQ](https://github.com/YOUR_USERNAME/3x-ui-auto-installer#readme)
2. Изучите существующие issues
3. Откройте новый issue с меткой "question"

## Код поведения

Мы ожидаем от всех участников:

- Уважительного отношения друг к другу
- Конструктивной критики
- Готовности помочь новичкам
- Фокуса на улучшении проекта

Спасибо за ваш вклад! 🙏
